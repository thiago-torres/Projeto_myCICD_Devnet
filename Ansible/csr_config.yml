---
- name: CSR Router Configuration
  hosts: csr
  connection: network_cli
  gather_facts: no
  tasks:
    - name: Configure loopback interface
      ios_config:
        parents:
          - interface Loopback102
        lines:
          - description Configured via Ansible - Thiago Torres - CICD Jenkins
          - ip address 10.10.100.102 255.255.255.255