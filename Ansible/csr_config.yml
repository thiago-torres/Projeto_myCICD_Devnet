---
- name: CSR Router Configuration
  hosts: csr
  connection: network_cli
  gather_facts: no
  tasks:
    - name: Configure loopback interface
      ios_config:
        parents:
          - interface Loopback103
        lines:
          - description Configured via Ansible - Thiago Torres - CICD Jenkins
          - ip address 10.10.100.103 255.255.255.255